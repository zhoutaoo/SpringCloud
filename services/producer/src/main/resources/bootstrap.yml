spring:
  application:
    name: producer
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: test
      fail-fast: false
      retry:
        max-attempts: 6