spring:
  application:
    name: producer
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: test
      fail-fast: true
      retry:
        max-attempts: 6